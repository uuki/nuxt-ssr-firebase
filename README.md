# nuxt-ssr-firebase

## Dependencies

- nuxt
- firebase-tools

## Setup

``` shell
$ cd app && yarn install && cd ../functions && npm i && cd ../ && npm i -g firebase-tools@8.4.3
```

## Deploy

``` shell
$ yarn deploy
```